\begin{table}[H]
    \centering
    \renewcommand{\arraystretch}{1.3}
    \caption{Core Functions of the SignalBranch Module}
    \begin{tabular}{p{5.2cm} p{10.2cm}}
        \toprule
        \textbf{Function Name} & \textbf{Description} \\
        \midrule
        validateSignal & Entry point for validating whether a signal can be cleared. Returns a ValidationResult object based on interlocking rules and real-time state. \\
        checkDependencies & Evaluates if other signals, routes, or linked interlocks satisfy logical dependencies (typically AND/OR combinations). \\
        getSignalAspect & Computes the final aspect to be shown on the signal, factoring in upstream signals, track segment status, and overlap clearance. \\
        logSignalDecision & Writes signal validation outcomes to the audit trail or event log for traceability and post-event analysis. \\
        \bottomrule
    \end{tabular}
    \label{tab:signal-branch-functions}
\end{table}
